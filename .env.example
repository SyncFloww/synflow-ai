# Django & Security
SECRET_KEY=
DEBUG=False
ALLOWED_HOSTS=
CORS_ALLOWED_ORIGINS=
FRONTEND_URL=
BACKEND_URL=

# JWT Configuration
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=15
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# Email Configuration
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@syncflow.ai

# Google OAuth Credentials (Google Identity Credential Flow)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional for Google Identity Credential Flow (used if redirect flow is configured in future)
GOOGLE_REDIRECT_URI=

# Testing Mode Override (True for local test suite execution)
TESTING=False
