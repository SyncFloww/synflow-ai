class AgentTools:

    def reply_to_comment(self, text):
        return f"Posting reply: {text}"

    def create_post(self, content):
        return f"Creating post: {content}"
        
    def send_dm(self, message):
        return f"Sending DM: {message}"
